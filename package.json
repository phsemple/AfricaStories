{
  "name": "africastorybook",
  "version": "1.0.0",
  "description": "African Stories from storybookscanada.ca",
  "main": "index.html",
  "scripts": {
    "devserver": "live-server public --port=3000 --no-browser",
    "watch:sass": "sass --watch public/sass/main.scss:public/styles/style.css",
    "dev": "npm-run-all --parallel watch:sass devserver ",
    "start": "serve public -l tcp://0.0.0.0:${PORT} --single --no-clean-urls"
  },
  "author": "Patrick Harley Semple",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.82.0"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
